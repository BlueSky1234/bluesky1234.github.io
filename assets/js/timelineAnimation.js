const leftContainers = Array.from(document.querySelectorAll('.timeline-content-container.left'));
const rightContainers = Array.from(document.querySelectorAll('.timeline-content-container.right'));

leftContainers.forEach(container => console.log(container));